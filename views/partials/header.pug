header.header
    .header__column
        a(href=routes.home) 
            i.fab.fa-youtube
    .header__column
        form(action=routes.search method="get")
            input(type="text" placeholder="Searching by.." name="term")
    .header__column
        ul
            li
                a(href=`/users${routes.userDetail}`) Profile
            li
                a(href=routes.join) Join
            li
                a(href=routes.login) Login